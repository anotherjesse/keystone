[zookeeper]
connection = localhost:2181

[identity]
driver = keystone.identity.backends.zk.Identity

[token]
driver = keystone.token.backends.kvs.Token

[ec2]
driver = keystone.contrib.ec2.backends.kvs.Ec2
